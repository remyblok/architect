{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run",
            "type": "shell",
            "command": "bundle exec jekyll serve --port 4000",
            "detail": "bundle exec jekyll serve --port 4000",
            "runOptions": {
                "runOn": "folderOpen"
            },
            "isBackground": true,
            "group": "build"
        }
    ]
}